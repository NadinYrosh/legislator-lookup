<div class="container">
  <button {{action 'showBillDetails'}}><h4>{{bill.official_title}}</h4></button>Date Introduced: {{bill.introduced_on}}
  <br>
  <br>
  {{#if showBill}}
    <p>Bill Sponsor: {{#link-to 'sponsor' bill.sponsor_id}}{{bill.sponsor.first_name}} {{bill.sponsor.last_name}}{{/link-to}}</p>
  {{/if}}
  <hr>
</div>
